{
  "name": "x402-crypto-mcp",
  "title": "x402 Crypto MCP",
  "description": "Crypto market data via x402: prices, sentiment, indicators, yields, gas, pools. 9 tools (stdio) / 10 tools (HTTP).",
  "version": "1.1.3",
  "author": {
    "name": "Yunare Maia"
  },
  "license": "MIT",
  "repository": {
    "url": "https://github.com/yunaremaia/x402-crypto-mcp",
    "source": "github"
  },
  "homepage": "https://github.com/yunaremaia/x402-crypto-mcp",
  "transport": {
    "type": "streamable-http",
    "url": "https://x402-mcp-server-two.vercel.app/api/mcp"
  },
  "capabilities": {
    "tools": {
      "listChanged": true
    }
  },
  "tools": [
    {
      "name": "get_price",
      "title": "Get Token Price",
      "description": "Get current price for any token on supported chains via x402",
      "inputSchema": {
        "type": "object",
        "properties": {
          "token": { "type": "string" },
          "chain": { "type": "string" },
          "network": { "type": "string", "enum": ["base", "ethereum", "arbitrum", "optimism", "polygon", "bsc", "avalanche", "solana"] }
        },
        "required": ["token"]
      }
    },
    {
      "name": "get_market_sentiment",
      "title": "Get Market Sentiment",
      "description": "Fear & Greed Index (Alternative.me) via x402",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "get_funding_rates",
      "title": "Get Funding Rates",
      "description": "Perpetual funding rates across exchanges via x402",
      "inputSchema": {
        "type": "object",
        "properties": { "symbol": { "type": "string" }, "exchange": { "type": "string" } }
      }
    },
    {
      "name": "get_technical_indicators",
      "title": "Get Technical Indicators",
      "description": "RSI, MACD, EMA, SMA for any symbol/timeframe via x402",
      "inputSchema": {
        "type": "object",
        "properties": {
          "symbol": { "type": "string" },
          "timeframe": { "type": "string" },
          "indicators": { "type": "array", "items": { "type": "string" } }
        },
        "required": ["symbol"]
      }
    },
    {
      "name": "get_defi_yields",
      "title": "Get DeFi Yields",
      "description": "Supply/borrow APY across protocols (Aave, Compound, Morpho, etc.) via x402",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chain": { "type": "string" },
          "protocol": { "type": "string" },
          "min_tvl": { "type": "number" },
          "min_apy": { "type": "number" }
        }
      }
    },
    {
      "name": "get_gas_price",
      "title": "Get Gas Price",
      "description": "EIP-1559 gas fees for EVM chains via x402",
      "inputSchema": { "type": "object", "properties": { "chain": { "type": "string" } } }
    },
    {
      "name": "get_multi_chain_gas",
      "title": "Get Multi-Chain Gas",
      "description": "Gas prices for multiple EVM chains at once via x402",
      "inputSchema": { "type": "object", "properties": { "chains": { "type": "array", "items": { "type": "string" } } } }
    },
    {
      "name": "get_pool_metrics",
      "title": "Get Pool Metrics",
      "description": "Yield pool APY/TVL/fee metrics across DEXes via x402",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chain": { "type": "string" },
          "dex": { "type": "string" },
          "min_apy": { "type": "number" },
          "min_tvl": { "type": "number" }
        }
      }
    },
    {
      "name": "get_pool_alerts",
      "title": "Get Pool Alerts",
      "description": "Pool alerts (APY spikes, new pools, TVL changes) via x402",
      "inputSchema": {
        "type": "object",
        "properties": { "chain": { "type": "string" }, "alert_type": { "type": "string" } }
      }
    },
    {
      "name": "scan_new_pairs",
      "title": "Scan New Pairs",
      "description": "Discover new AMM pairs on DEXes via x402",
      "inputSchema": {
        "type": "object",
        "properties": { "chain": { "type": "string" }, "dex": { "type": "string" }, "min_liquidity": { "type": "number" } }
      }
    }
  ]
}